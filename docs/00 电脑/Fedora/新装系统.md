更新系统
```bash
sudo dnf update
```

安装编解码器
```bash
sudo dnf group install multimedia
```

GitHub hosts

雾凇输入法
```
 sudo dnf install librime-lua
```
 皮肤怎么搞
 

V2rayA


chrome
obsidian
flathub镜像

![[Pasted image 20251123235915.png]]


输入法皮肤
用户主题目录
~/.local/share/fcitx5/themes/


smplay     

bottle乱码
安装cjkfonts
环境变量设置
LANG	zh_CN.UTF-8
LC_ALL	zh_CN.UTF-8
LC_CTYPE	zh_CN.UTF-8
WINEDLLOVERRIDES	gdi32=n,b


1. 永久生效（推荐）：编辑系统配置文件，避免重启后恢复默认。执行`sudo nano /etc/sysctl.conf`，在文件末尾添加`vm.swappiness=10`，保存退出后，再执行`sudo sysctl -p`让配置立即生效。

qq音乐乱码
en_US.UTF-8
```
dnf install google-noto-sans-cjk-fonts google-noto-serif-cjk-fonts
```