# debian重装
## 一.镜像源设置
nano /etc/apt/sources.list
```
# 默认注释了源码仓库，如有需要可自行取消注释
deb http://mirrors.ustc.edu.cn/debian trixie main contrib non-free non-free-firmware
# deb-src http://mirrors.ustc.edu.cn/debian trixie main contrib non-free non-free-firmware
deb http://mirrors.ustc.edu.cn/debian trixie-updates main contrib non-free non-free-firmware
# deb-src http://mirrors.ustc.edu.cn/debian trixie-updates main contrib non-free non-free-firmware

# backports 软件源，请按需启用
# deb http://mirrors.ustc.edu.cn/debian trixie-backports main contrib non-free non-free-firmware
# deb-src http://mirrors.ustc.edu.cn/debian trixie-backports main contrib non-free non-free-firmware
```

## 二.普通用户使用sudo
```bash
su -
apt update && apt install sudo -y
usermod -aG sudo 你的用户名
exit
```
重新连接ssh

## 三.安装cockpit
https://cockpit-project.org/running.html#debian
host:9090
### 官方组件
1. Files 文件管理器，Cockpit 官方文件浏览器。（仅 Debian 13 可用，Debian 12 无此包）
```
. /etc/os-release
apt install -t ${VERSION_CODENAME}-backports cockpit-files -y
```
### 第三方组件
45Drives Repo


## 四.挂载硬盘

## 五.系统优化
### 1.交换空间优化
```bash
sudo vim /etc/sysctl.conf
```
添加
```bash
vm.swappiness=10
vm.vfs_cache_pressure=50
```
### 2.安装Tuned系统调优工具
```bash
sudo apt install tuned
```
## 六.代理
export http_proxy=http://192.168.31.112:7890
export https_proxy=http://192.168.31.112:7890

## 七.安装docker
```bash
# Add Docker's official GPG key:
sudo apt-get update
sudo apt-get install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/debian/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# Add the repository to Apt sources:
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/debian \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt-get update
```
### 1.配置镜像源



### 2.设置docker代理
1. **创建配置目录**
在终端中运行以下命令，创建 Docker 服务的 systemd 配置文件夹：
```bash
sudo mkdir -p /etc/systemd/system/docker.service.d
```
2. **创建并编辑代理配置文件**
使用编辑器创建 http-proxy.conf 文件：
```bash
sudo nano /etc/systemd/system/docker.service.d/http-proxy.conf
```
写入
```conf
[Service]
Environment="HTTP_PROXY=http://192.168.31.112:7890"
Environment="HTTPS_PROXY=http://192.168.31.112:7890"
Environment="NO_PROXY=localhost,127.0.0.1,::1"
```
3. **刷新配置并重启Docker**
```
sudo systemctl daemon-reload
sudo systemctl restart docker
```
4. **验证是否生效**
```bash
docker info | grep -i proxy
```
