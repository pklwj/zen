
```nginx
nginx.conf
Generated by nginxconfig.io
See nginxconfig.txt for the configuration share link

user                 nginx; #定义Nginx工作进程运行的用户和用户组（`nginx`），避免使用root用户以增强安全性。
pid                  /var/run/nginx.pid;  #指定主进程PID文件存储路径（`/var/run/nginx.pid`），用于服务管理。
workerprocesses     auto;#- 设置为`auto`时自动匹配CPU核心数，优化多核CPU利用率（如4核CPU启动4个工作进程）。
workerrlimitnofile 65535;#- 设置单个工作进程最大文件句柄数（65535），需配合系统`ulimit -n`调整。

Load modules
include              /etc/nginx/modules-enabled/.conf;

events {
multiaccept       on;#开启后（on）允许每个工作进程一次性接受所有新连接请求，提升高并发场景性能。
workerconnections 65535;#- 单个工作进程最大并发连接数（65535），需满足：`worker_processes * worker_connections >= 总并发量`。
}

http {
charset                utf-8;
sendfile               on;#- 启用零拷贝技术（`on`），直接在内核空间完成文件传输，减少用户态与内核态数据拷贝。
tcpnopush             on;#- 与`sendfile on`配合使用，优化TCP数据包发送策略，提升网络吞吐量。
tcpnodelay            on;
servertokens          off;
lognotfound          off;
typeshashmaxsize    2048;
typeshashbucketsize 64;
clientmaxbodysize   16M;#- 允许客户端上传的最大文件大小（16MB），适用于文件上传场景，需根据业务需求调整。

# MIME
include                mime.types;
defaulttype           application/octet-stream;

# Logging
accesslog             off;
errorlog              /dev/null;

# SSL
sslsessiontimeout    1d;
sslsessioncache      shared:SSL:10m;#- 使用共享内存缓存SSL会话（`shared:SSL:10m`），减少TLS握手开销，提升性能。
sslsessiontickets    off;

# Diffie-Hellman parameter for DHE ciphersuites
ssldhparam            /etc/nginx/dhparam.pem;

# Mozilla Intermediate configuration
sslprotocols          TLSv1.2 TLSv1.3;#- 禁用TLSv1.0/1.1等不安全协议，仅允许TLSv1.2+，符合PCI DSS安全标准。
sslciphers            ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;

# OCSP Stapling
sslstapling           on;
sslstaplingverify    on;
resolver               1.1.1.1 1.0.0.1 8.8.8.8 8.8.4.4 208.67.222.222 208.67.220.220 valid=60s;
resolvertimeout       2s;

# Connection header for WebSocket reverse proxy
map $httpupgrade $connectionupgrade {#- 通过`$http_upgrade`映射实现WebSocket协议升级（`Connection: upgrade`）。
default upgrade;
""      close;
}
proxy_set_header Host $host;#- 传递客户端真实IP（`X-Real-IP`）和域名（`Host`），解决反向代理后的信息丢失问题。
proxy_set_header X-Real-IP $remote_addr;


map $remoteaddr $proxyforwardedelem {

# IPv4 addresses can be sent as-is
~^[0-9.]+$        "for=$remoteaddr";

# IPv6 addresses need to be bracketed and quoted
~^[0-9A-Fa-f:.]+$ "for=\"[$remoteaddr]\"";

# Unix domain socket names cannot be represented in RFC 7239 syntax
default           "for=unknown";
}

map $httpforwarded $proxyaddforwarded {

# If the incoming Forwarded header is syntactically valid, append to it
"~^(,[ \\t])([!#$%&'+.^|~0-9A-Za-z-]+=([!#$%&'+.^|~0-9A-Za-z-]+|\"([\\t \\x21\\x23-\\x5B\\x5D-\\x7E\\x80-\\xFF]|\\\\[\\t \\x21-\\x7E\\x80-\\xFF])\"))?(;([!#$%&'+.^|~0-9A-Za-z-]+=([!#$%&'+.^|~0-9A-Za-z-]+|\"([\\t \\x21\\x23-\\x5B\\x5D-\\x7E\\x80-\\xFF]|\\\\[\\t \\x21-\\x7E\\x80-\\xFF])\"))?)([ \\t],([ \\t]([!#$%&'+.^|~0-9A-Za-z-]+=([!#$%&'+.^|~0-9A-Za-z-]+|\"([\\t \\x21\\x23-\\x5B\\x5D-\\x7E\\x80-\\xFF]|\\\\[\\t \\x21-\\x7E\\x80-\\xFF])\"))?(;([!#$%&'+.^|~0-9A-Za-z-]+=([!#$%&'+.^|~0-9A-Za-z-]+|\"([\\t \\x21\\x23-\\x5B\\x5D-\\x7E\\x80-\\xFF]|\\\\[\\t \\x21-\\x7E\\x80-\\xFF])\"))?))?)$" "$httpforwarded, $proxyforwardedelem";

# Otherwise, replace it
default "$proxyforwardedelem";
}

# Load configs
include /etc/nginx/conf.d/.conf;
include /etc/nginx/sites-enabled/;
}
```
