更新系统
```bash
sudo dnf update
```

安装编解码器
```bash
sudo dnf group install multimedia
```

清除缓存
```
sudo dnf clean all
sudo dnf makecache
```

windows远程连接fedora
用自带的远程桌面

Flatpak加速
如果之前从未使用过 Flathub，那么首先需要添加 Flathub 远程源：
```bash
flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo
```
在已有 `flathub` 远程源的基础上：
```bash
sudo flatpak remote-modify flathub --url=https://mirror.sjtu.edu.cn/flathub
```
恢复默认值：
```bash
sudo flatpak remote-modify flathub --url=https://dl.flathub.org/repo
```

> 出现问题
> https://mirrors.sjtug.sjtu.edu.cn/docs/flathub



switch
https://prodkeys.net/


# 10 Things to do After Installing Fedora 43 (Workstation)
https://www.debugpoint.com/10-things-to-do-fedora-43-after-install/#Make_dnf_faster

雾凇输入法
https://github.com/iDvel/rime-ice

# Disable NetworkManager-wait-online.service
Disabling it can decrease the boot time by at least ~15s-20s:
```bash
sudo systemctl disable NetworkManager-wait-online.service
```

# 安装fuse
```
dnf install fuse fuse-libs
```